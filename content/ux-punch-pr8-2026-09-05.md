# UX punch list — PR #8
Date: 2026-09-05 America/Sao_Paulo
Source: UX. Cloud implements.

## Blockers
1. Forensic micro-card clusters
2. Filters write URL query string
3. Public preview URL (vercel 404)

## Also: EventCard publisher, PT tags, summary clamp, demote confidence filter, month heads, hero teaser to metodologia, ficha prev/next, people search+pin, network mobile list-first + legend + ?pessoa=, metodologia #ressalvas, export JSON+CSV only, mobile nav disclosure.

Voice: paper/oxblood. Clusters = notes not Kanban.


## Cluster concrete (PM/UX 5 Sep)
- Day ≥2 events → cluster; month soft ≥3 same tag family if no day precision; never cross year; no fake cluster of 1
- Closed: date + label + “N fichas” + stacked micro-rows (title·evidence·publisher); one confidence chip if any low/med; expand button; paper/oxblood not Kanban
- Open: full EventCards chrono; `?dia=` / child URL for share
- Mobile: full width 44px tap, no carousel
- Density: year+month heads; clusters collapsed default; Expandir tudo off
